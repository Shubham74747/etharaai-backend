services:
  - type: web
    name: hrms-backend
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: DATABASE_URL
        sync: true
    # health check used by Render to detect healthy containers
    healthCheckPath: /api/health
